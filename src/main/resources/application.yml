server:
  address: localhost
  port: 8080

spring:
  thymeleaf:
    prefix: classpath:templates/
    suffix: .html
    cache: false

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/opensourceproject?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 102030zz**

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect