server:
    address: localhost
    port: 8080

spring:
    thymeleaf:
        prefix: classpath:templates/
        suffix: .html
        cache: false

    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://183.111.227.104:3306/test?useSSL=false&characterEncoding=utf8&serverTimezone=UTC&zeroDateTimeBehavior=convertToNull
        username: test
        password: Foodie123!@#

mybatis:
    type-aliases-package: com.example.demo.dto
    mapper-locations: classpath:mappers/*.xml
    configuration:
        map-underscore-to-camel-case: true

logging:
    level:
        com.example.demo.mapper: debug